{
  "name": "mohan-cv",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Print-perfect CV as code: typed per-language content files, one stylesheet, reproducible PDF export.",
  "scripts": {
    "dev": "bun server.ts",
    "build": "bun build src/main.ts --outfile=dist/cv.js --format=iife",
    "check": "bunx tsc --noEmit"
  },
  "devDependencies": {
    "bun-types": "^1.3.0",
    "typescript": "^5.5.0"
  }
}
